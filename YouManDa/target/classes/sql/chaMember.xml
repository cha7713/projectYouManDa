<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	

	<insert id="join" parameterType="map">
		insert into `join` (id, pw, name, address, email,school,phonenum,passnum,sex,birthday,nick,lang)
		values
		(#{id},(HEX(AES_ENCRYPT(#{pw}, 'hgr'))),#{name},#{address},#{email},#{school},#{phonenum},#{passnum},#{sex},#{birthday},#{nick},#{lang})
	</insert>


</mapper>