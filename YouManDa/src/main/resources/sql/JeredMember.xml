<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="selectUser" resultType="map" parameterType="_int">
		SELECT 
		 id, lang, pw, email, school, address, phonenum, passnum, sex, name, birthday, nick
		 FROM MEMBER
		 WHERE id = #{value}
	</select>

</mapper>